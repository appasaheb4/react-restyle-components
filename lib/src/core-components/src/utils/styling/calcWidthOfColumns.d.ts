export type CalcWidthOfColumnsArgs = {
    columns: number;
    totalColumns: number;
    margin: number;
    gutter: number;
    maxWidth?: string;
};
export declare const calcWidthOfColumns: ({ columns, totalColumns, margin, gutter, maxWidth, }: CalcWidthOfColumnsArgs) => string;
//# sourceMappingURL=calcWidthOfColumns.d.ts.map